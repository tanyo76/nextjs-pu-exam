import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Add book",
  description: "Page, which allows you to create a blog article",
};



export default function AddBookPage() {
  return (
    <section>
      <h1>Add book</h1>
    </section>
  );
}
